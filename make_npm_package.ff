import ff.kitt.Module as kitt
import ff.kitt.archive as archive
import ff.kitt.os as os

#run {
    await > kitt:install { false "" "kitt" }
    await > os:exec > "npm publish"
    "Kitt Npm Package created"
}